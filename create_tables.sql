CREATE TABLE producto (
    nombre VARCHAR(100) PRIMARY KEY,
    temporada VARCHAR(50),
    link_foto TEXT,
    continente VARCHAR(50),
    equipo VARCHAR(100),
    liga VARCHAR(100),
    tipo VARCHAR(50)
);